export struct CatInfo {
    category: string,
    full: string,
    first: string,
    with_hint: bool,
}
  
export struct FullUpdate  {
    num: int,
    out_of: int,
    score: int,
    flag: image,
    seen: bool, 
}